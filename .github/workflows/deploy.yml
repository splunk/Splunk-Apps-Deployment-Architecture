name: Deploy Splunk Apps

on:
  pull_request:
    types:
      - closed
    branches:
      - main
    paths:
      - environments/**
jobs:
  download-and-deploy:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    strategy:
      matrix:
        environment:
          - name: "test/stg"
            token_key: "SPLUNK_TOKEN_TEST_STG"
          - name: "test/es"
            token_key: "SPLUNK_TOKEN_TEST_ES"
          - name: "prod/es"
            token_key: "SPLUNK_TOKEN_PROD_ES"
          - name: "prod/stg"
            token_key: "SPLUNK_TOKEN_PROD_STG"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.9
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --upgrade pyyaml
          pip install boto3 requests schema ksconf
      - name: Deploy to ${{ matrix.environment.name }}
        continue-on-error: true
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          SPLUNK_USERNAME: ${{ secrets.SPLUNK_USERNAME }}
          SPLUNK_PASSWORD: ${{ secrets.SPLUNK_PASSWORD }}
          SPLUNK_TOKEN: ${{ secrets[matrix.environment.token_key] }}
          DEPLOYMENT_CONFIG_PATH: "environments/${{ matrix.environment.name }}"
        run: |
          echo "Deploying to ${{ matrix.environment.name }} environment"
          python -u deploy.py
      - name: Upload deployment report as artifact
        uses: actions/upload-artifact@v4
        with:
          name: deployment-report
          path: artifacts/
      - name: Prepare sanitized artifact name
        id: sanitize
        run: |
          TIMESTAMP=$(date '+%Y%m%d-%H%M%S')
          echo "SANITIZED_NAME=${{ matrix.environment.name }}-$TIMESTAMP" | sed 's/[\/:*?"<>|]/-/g' >> $GITHUB_ENV
      - name: Debugging apps re-package
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.SANITIZED_NAME }}-tgz-files
          path: ./*.tgz

